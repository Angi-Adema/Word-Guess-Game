//Button to start the game and thus a timer.

//Timer started for the user to guess the word within 5 seconds of starting the time.
var secondsLeft = 5;

function setTime() {
    var timeInterval = setInterval function () {
        secondsLeft--;
        
    }
}

//Event listener to enter user key input.